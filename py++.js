var err0r = "There is an error in the path";
function print(text) {
  document.write(text)
};

function retrn(text) {
  console.log(text);
};

function def(variableName,text,text2) {
  if(text){
    text
  } else {
     // for(let i = 0; i < 5; i++) {
      retrn(err0r + " " + variableName + ".")
    //  };
  }
};